import { Header } from "../Components";

const Checkout = () => {
    return (
        <div className="bg-gray-700">
            <h1>Checkout Page</h1>
        </div>
    );
};

export default Checkout;